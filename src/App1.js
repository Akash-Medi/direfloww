const component = document.querySelector("first-component");
component.addEventListener("click", () => {
  alert("hello i'm react props");
});

// const btn = (document.createElement("button").innerHTML = "react");
// component.appendChild(btn);
